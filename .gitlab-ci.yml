heroku:
  stage: deploy
  before_script:
    - uname -a
    - cat /etc/lsb-release
    - git remote add heroku https://heroku:$HEROKU_TOKEN@git.heroku.com/comtoise.git
  script:
      git push heroku master
  only:
      - master